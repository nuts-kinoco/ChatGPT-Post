# Decision Log

軽微な判断・仮定の記録。大きな設計判断は `docs/adr/` の ADR に記録する。
形式: ID / 日付 / 種別（Assumption = 仮定, Decision = 判断）/ 内容 / 根拠 / 影響 / 状態。

| ID | 日付 | 種別 | 内容 | 根拠 | 影響 | 状態 |
|---|---|---|---|---|---|---|
| A-001 | 2026-09-14 | Assumption | リポジトリを `S:\Projects\chatgpt-web-bridge` に新規作成した | セッションがプロジェクト未選択で起動され、PO の他プロジェクトが `S:\Projects` 配下にあった。既存の同名フォルダは無し | — | **確認済み**（PO が Phase 2 続行を指示、異議なし） |
| A-002 | 2026-09-14 | Decision | パッケージマネージャは npm | pnpm / yarn が未インストール。マスタープロンプトは「どちらか一つ」を要求 | `package-lock.json` のみ | 決定 |
| A-003 | 2026-09-14 | Decision | Git ブランチは `phase-1/requirements` で作業し、`main` には触れない | OPS-003。空リポジトリのため `main` はまだコミットを持たない | PO の指示があればコミット・マージ | 決定 |
| A-004 | 2026-09-14 | Assumption | OpenAI 利用規約の内容は PO 調査（2026-09-14）を正とする | Claude の自動取得が HTTP 403 | OQ-001 で PO が手動確認 | 仮定 |
| A-005 | 2026-09-14 | Decision | Phase 1 では文書のみ作成し、`package.json` / `tsconfig.json` も作らない | マスタープロンプト「実装コード、Playwright selector、本番 CLI はまだ書かない」「最小限のプロジェクト骨格と docs/ だけ」 | 骨格は空ディレクトリ（`docs/adr`, `reviews`, `schemas`, `examples`）と `.gitkeep` | 決定 |
| A-006 | 2026-09-14 | Decision | 文書は日本語、ID・コード・ファイル名は英語 | PO とのやり取りが日本語。Codex レビューは英語でも読める | Phase 3 のレビュー依頼は日本語文書を対象にする | 決定 |
| A-007 | 2026-09-14 | Assumption | Codex レビューの既定モデルは環境既定 `gpt-5.6-terra`、effort high | `~/.codex/config.toml` の現在値 | Phase 3 実行前に `codex exec --help` と利用可能モデルを再確認 | 仮定 |
| A-008 | 2026-09-14 | Decision | 終了コード: 0=completed, 1=起動後の失敗・送信状態不明, 2=不正な入力・設定, 3=manual_intervention_required, 4=起動前停止（何も送信されていない。result.json の有無は混在） | 呼び出し元が「再送してよいか」「人間が必要か」「何も起きていないか」を終了コードだけで判別できるようにする | `13-ERROR-MODEL.md` §3 で確定（A-032 で `BROWSER_LAUNCH_FAILED` / `INVALID_CONFIG` 追加。「新しい結果も無い」の文言は撤回） | 確定（13 §3 に置換） |
| A-009 | 2026-09-14 | Decision | MVP では `newChat: false` を `INVALID_REQUEST` とする | FR-091（会話継続は Won't）。フィールド自体は将来のために契約に残す | 契約の `newChat` は必須かつ `true` のみ有効 | 決定 |
| A-010 | 2026-09-14 | Decision | 成功時は Playwright trace を既定で保存しない（設定で有効化可） | trace は秘密情報を含み得る（R-009）。失敗時のみ必要 | AC-025 | 決定 |
| A-011 | 2026-09-14 | Decision | FR-014（処理済み requestId の冪等拒否）と FR-032（抽出方式・品質の記録）を Should から Must に昇格 | 自己検証で「Critical リスク R-003 の緩和策が Should に依存」「result.json のフィールド定義と不整合」の指摘。いずれも実装コストが低く呼び出し元の安全性に直結 | AC-011, AC-022 | 決定 |
| A-012 | 2026-09-14 | Decision | `RATE_LIMITED` は `manual_intervention_required`（終了コード 3）、`CHAT_ERROR` は `failed`（終了コード 1） | CON-011「利用上限は手動介入ステータスで停止」。自己検証で status が一意でない指摘 | FR-029, AC-021, §7 表 | 決定 |
| A-013 | 2026-09-14 | Decision | `preset: "current"` でも UI 表示から preset を観測できなければ `MODEL_NOT_VERIFIABLE` で送信しない | CON-007 の抜け穴を塞ぐ。Phase 4 縦切りは `current` を使うため初期から影響 | FR-020, FR-016(e), AC-015 | 決定 |
| A-014 | 2026-09-14 | Decision | `ALREADY_RUNNING` = ブリッジ自身の single-flight ロック取得失敗、`PROFILE_IN_USE` = ブリッジ外プロセスによる占有で Playwright 起動失敗。process ロックは `run` / `login` / `inspect-ui` / ブラウザを起動する `doctor` 項目すべてが取得 | 自己検証で 2 コードの判定基準が一意でない指摘 | FR-019, FR-035, AC-014, AC-024 | 決定 |
| A-015 | 2026-09-14 | Decision | Phase 1 の自己検証は 3 視点（ID 整合性 / 絶対条件への忠実性 / 検証可能性と矛盾）の独立エージェントで 2 ラウンド行い、第 1 ラウンド 30 件・第 2 ラウンド 25 件（重複含む）の指摘をすべて反映した | マスタープロンプト「内容の整合性、要件 ID、受入条件を検証」。Codex は Phase 1 では使用しない規定 | 指摘原文は `reviews/phase1-self-review-round1.json` / `round2.json`、要約は `reviews/phase1-self-review.md` | 実施済み |
| A-016 | 2026-09-14 | Decision | `ALREADY_PROCESSED` と `ALREADY_RUNNING` は `result.json` を書かず、終了コード 4 と標準エラーのみで通知する | 既存または進行中の `result.json` を敗者プロセスが上書きすると呼び出し元が誤読する（R-011）。「常に result.json を書く」と「既存を上書きしない」は両立しない | FR-010 例外 (2)(3)、FR-014、FR-036、AC-007、AC-011、AC-024 | 決定 |
| A-017 | 2026-09-14 | Decision | 処理順序: request.json 読取 → FR-014 冪等判定 → スキーマ検証 → ロック取得 → ブラウザ起動 | 冪等判定とスキーマ検証の優先順位が未定義だった。既存結果の保護を最優先にする | FR-010、AC-006 | 決定 |
| A-018 | 2026-09-14 | Decision | 恒久的禁止事項（内部 API、自動ログイン、CAPTCHA / bot 回避、偽装、Cookie/HAR 抽出）を「Won't for MVP」から分離し `00` §5.2a に置く | 「MVP では行わない」と読めると絶対条件が軟化する | 00 §5.2a、FR-094/095 | 決定 |
| A-019 | 2026-09-14 | Decision | NFR-001 を PowerShell 5.1（Must）と NFR-001a PowerShell 7（Could、OQ-008 で確定）に分割 | PS7 が現環境に無く、Must のまま残すと検証不能な Must になる | AC-027 | 決定 |
| A-020 | 2026-09-14 | Decision | ブラウザは既定でインストール済み Google Chrome（`channel: 'chrome'`）、環境変数で Playwright 同梱 Chromium に切替可 | OQ-004。ログインフロー（SSO）が自動化 Chromium で拒否される事例があり、通常利用に近い方を既定にする。再現性が必要な検証は Chromium | ADR-002、`doctor` がチャネルと版を表示 | 決定 |
| A-021 | 2026-09-14 | Decision | Lint/Format は Biome | OQ-005。単一ツールで完結、設定が少ない | `biome.json`、禁止 import の機械検査にも利用 | 決定 |
| A-022 | 2026-09-14 | Decision | `requestId` は呼び出し元が生成。形式 `^[A-Za-z0-9][A-Za-z0-9._-]{7,63}$`、推奨 `<yyyyMMddTHHmmssZ>-<8 hex>` | OQ-006。UUID v7 ライブラリを避け、PowerShell だけで時系列 ID を作れる | `schemas/request.schema.json`、ADR-003 | 決定 |
| A-023 | 2026-09-14 | Decision | 「コピー」操作の捕捉はページ内フック（`navigator.clipboard.writeText` / `write` を `addInitScript` で差し替え）で行い、システムクリップボードを読み書きしない | OQ-007。PO の作業との衝突と他アプリの上書きによる誤取得を根本から消す。PO 判断が不要になった | ADR-004、15-SECURITY §6（製品コード唯一のページ内スクリプト） | 決定 |
| A-024 | 2026-09-14 | Decision | 送信直前に `submit.marker` を write-ahead で書き、次回起動時に marker のみ残存なら `SUBMIT_STATE_UNKNOWN`（終了コード 1、`submitted: unknown`）で停止する | クラッシュ耐性のある二重送信防止。マーカーは成功時も残す | ADR-005、FR-014、FR-025、11-STATE-MACHINE §6 | 決定 |
| A-025 | 2026-09-14 | Decision | `result.json` に `submitted: yes/no/unknown` と `bridgeVersion` を追加。エラーコードに `SUBMIT_STATE_UNKNOWN` と `INTERNAL_ERROR` を追加 | 呼び出し元が「送信された可能性」を判断できる必要がある。未分類例外の受け皿が必要 | 12-IO-CONTRACT、13-ERROR-MODEL、`schemas/result.schema.json` | 決定 |
| A-026 | 2026-09-14 | Decision | `result.json` / `response.md` の出力先は `request.json` と同じディレクトリ。`--output-dir` は設けない | 冪等判定を単純にする。OPS-010 | ADR-003 | 決定 |
| A-027 | 2026-09-14 | Decision | （旧）完了検出の既定値: 初回応答待ち 120 s を含む | — | A-029 で置換（初回応答上限は廃止） | 置換済み |
| A-028 | 2026-09-14 | Decision | trace はサニタイズしてから保存（`*.network` と会話 API 応答本体を zip から除去、残存エントリの cookie/authorization を検査） | SEC-010。Playwright の trace オプションだけではネットワークヘッダーを除外できない可能性があるため後処理で担保 | 15-SECURITY §3、AC-025 | 決定 |
| A-029 | 2026-09-14 | Decision | 完了検出の既定値を改訂: 観測間隔 250 ms、安定化 1500 ms（**停止ボタン消失を起点**）、代替経路 5000 ms + 送信ボタン復帰 + メッセージ単位コピー可用、`timeoutMs` を他のあらゆる待機より優先、固定の初回応答上限（旧 120 s）は廃止。送信前フェーズ上限 60/30/30/60 s、`context.close()` 上限 15 s | 自己検証で「規則順序により timeoutMs が効かない」「安定化の起点が曖昧」「初回応答 120 s に要件根拠が無い」の指摘。A-027 を置換 | 10 §6 / §9、11 §5、AC-019 / AC-020 | 暫定（Live で調整） |
| A-030 | 2026-09-14 | Decision | `timeoutMs` は任意（省略時 900000） | FR-007 の必須リストと schema が食い違っていた。呼び出し元の負担を減らす設計側に合わせる | FR-007, 12 §2, request.schema.json | 決定 |
| A-031 | 2026-09-14 | Decision | FR-036「requestId 単位のロック」はプロセスロック + requestId でグローバルな `runtime/state/<requestId>/submit.marker` + `requestDir/result.json` の存在判定で実現し、別ファイルの requestId ロックは持たない。marker 判定はロック取得後に行う | 自己検証 Critical: marker 判定がロック前だと同一 requestId の二重起動で敗者が `SUBMIT_STATE_UNKNOWN` を書き再送を誘発。requestDir 依存だと別ディレクトリで同一 requestId が再送される | FR-010 順序（A-017 改訂）、FR-036、11 §4 / §6、ADR-005 | 決定 |
| A-032 | 2026-09-14 | Decision | エラーコードに `INVALID_CONFIG`（プロファイルパス拒否、終了コード 2）と `BROWSER_LAUNCH_FAILED`（プリチェック通過後の起動例外、終了コード 4、trace 無し）を追加。`BROWSER_CRASHED` は起動後のみ | 起動前失敗を `INVALID_REQUEST` / `BROWSER_CRASHED` に混ぜると案内・終了コード群・trace 要件が矛盾する | 13、02 §7、result.schema.json、AC-005 / AC-014 / AC-025 | 決定 |
| A-033 | 2026-09-14 | Decision | 存在検出用要素（`presence` モード）は全候補を OR 評価し 0 件でも `DOM_CHANGED` にしない。操作対象（`unique`）のみ exactly-one を要求。送信後は `DOM_CHANGED` を発生させず抽出の降格で扱う。`copyTurnButton` はコードブロックの Copy を除外 | 自己検証 High: 第 1 候補 0 件でフォールバックが死ぬ、コードブロック 2 個以上で `DOM_CHANGED` になる | 14 §1 / §3、FR-041、AC-026 | 決定 |
| A-034 | 2026-09-14 | Decision | 「続きを生成」表示は `CHAT_ERROR(output_truncated)`（自動クリックしない）、送信後の assistant ターン増分 2 以上は `CHAT_ERROR(multiple_responses)`、Canvas 等のサイドパネルは `EXTRACTION_FAILED(canvas)`、空本文は完了判定を通した後に `EXTRACTION_FAILED(empty)` | 自己検証 High: 途中で切れた回答・A/B 回答・要約本文が `completed` になる。空回答が `EXTRACTION_FAILED` に到達できない | 10 §6 / §7、13 §4、FR-029 / FR-034 | 決定 |
| A-035 | 2026-09-14 | Decision | copy-capture シムは `run` のコンテキストにのみ登録し、`login` / `doctor` / `inspect-ui` では登録しない | 人間がログイン画面でコピーする操作（2FA コード等）を妨げない | ADR-004、15 §6 | 決定 |
| A-036 | 2026-09-14 | Decision | 失敗時の effects 順序は `CAPTURE → STOP_TRACE → WRITE_RESULT → CLOSE_BROWSER → RELEASE_LOCK`。成功時も trace 保存が有効なら `WRITE_RESULT` の前に `STOP_TRACE`。ロック解放は必ずブラウザ終了の後 | `artifacts` に列挙するファイルは `result.json` より先に存在する必要がある。ロックを先に解放すると次の `run` が `PROFILE_IN_USE` になる | 11 §4、10 §4 / §5、FR-035 / FR-039 | 決定 |
| A-037 | 2026-09-14 | Decision | Phase 2 の自己検証は 3 視点（要件トレーサビリティ / 内部整合性 / 敵対的安全性）で行い、ラウンド 1 の 59 件（Critical 1・High 10・Medium 22・Low 26）をすべて反映した | マスタープロンプトの Phase 2 出口条件。Codex は Phase 3 | `reviews/phase2-self-review-round1.json`、`reviews/phase2-self-review.md` | 実施済み |
| A-038 | 2026-09-14 | Decision | ロックに所有トークンを持たせ、取得直後・`submit.marker` 書込直前（`VERIFY_LOCK`）・解放時に再検証する。stale 回収は内容照合付き rename（他者の生きた lock を奪ったら元に戻す）。失敗は `LOCK_LOST` → `ALREADY_RUNNING`（送信なし、result.json 無し） | 自己検証ラウンド 2 High: rename 回収の直後に別プロセスが新 lock を rename できる二次 TOCTOU。送信直前の再検証で「奪われた側は送信しない」を保証 | ADR-005、FR-037、11 §4 / §6、13 | 決定 |
| A-039 | 2026-09-14 | Decision | `composerReady` = 「`stopButton` 不在 かつ `composer` が可視・編集可能」。送信ボタンの有効/無効は使わない。送信後の観測は例外を投げない読み取り API（`probe` / `exists` / `countMatches`）のみ | ラウンド 2 High: ChatGPT は入力欄が空だと送信ボタンを無効化するため、旧定義では正常系が完了しない。生成中は送信ボタンが停止ボタンに置換され unique 解決が失敗する | 10 §6、14 §1 / §3 / §5、16 | 決定（Phase 4 で実画面確認） |
| A-040 | 2026-09-14 | Decision | `sendButton` は送信境界前（`SNAPSHOT_BASELINE`）に解決して保持し、`PROMPT_SUBMITTING` では `DOM_UNEXPECTED` を発生させない（消失は `SUBMIT_FAILED` → `PROMPT_SUBMIT_FAILED`、`submitted: unknown`）。`DOM_CHANGED` の発生範囲は `PROMPT_ENTERED` まで | `PROMPT_SUBMITTING` での `DOM_CHANGED` が `submitted` の不変条件と矛盾していた | 11 §1 / §4 / §6、13 §2 / §4、14 §5 | 決定 |
| A-041 | 2026-09-14 | Decision | 安定化窓の起点 = max(停止ボタン消失時刻, 本文ハッシュ最終変化時刻)。送信後 3 状態 × `VERDICT_*` 9 種を遷移表で全定義（一過性の `VERDICT_WAITING` は継続扱い）。`STOP_OBSERVATION_LOOP` effect を追加 | ラウンド 2: 消失後の後描画で永久待機、SPA 再描画の一過性 0 件で `INTERNAL_ERROR` | 10 §6、11 §4 | 決定 |
| A-042 | 2026-09-14 | Decision | 成功経路の effects を `WRITE_RESPONSE_MD, STOP_TRACE(if enabled), WRITE_RESULT` と明示。`SNAPSHOT_BASELINE` と `WRITE_SUBMIT_MARKER` を 2 段の遷移に分離。controller は effects を順に実行しイベント発生で残りを中断する規約 | 表駆動テストで成功経路と marker 未書込の判定が導けなかった | 11 §1 / §4、16 | 決定 |
| A-043 | 2026-09-14 | Decision | `modelPickerOption` は count モード（0 件 = `MODEL_NOT_AVAILABLE`、2 件以上 = `MODEL_NOT_VERIFIABLE`）。preset ラベル照合は前後アンカー付き完全一致。`presence` は可視一致、`count` は attached 数。`openNewChat` は `composer` 出現時点で最終判定。`observeAuth` に `NOT_READY` を追加。`enterPrompt` の再入力は内部で最大 2 回 | ラウンド 2 の実装判断漏れ | 14 §1 / §3 / §5 / §6、11 §3 / §5、13 §4 | 決定 |
| A-044 | 2026-09-14 | Decision | trace サニタイザは `.network` を削除ではなく縮約（URL はクエリ・フラグメント除去、ヘッダー・postData 無し）、`trace.trace` は行単位で `redact()` 置換（エントリ削除しない）、スクリーンキャストは許可 | ラウンド 2: 削除方式では CSS が適用されず、1 か所の JWT で trace 全体が消え、AC-025「開ける」に反する | 15 §3、AC-025、16 | 決定 |
| A-045 | 2026-09-14 | Decision | `requestId` は末尾英数字に限定し Windows 予約デバイス名を拒否。会話 ID（`/c/<uuid>`）は秘密情報ではない（fixture では固定ダミーに置換するが grep 対象外）。`UPDATE_MARKER`（送信後の追記）は best-effort。抽出の入口で本文空なら即 `EXTRACTION_FAILED(empty)`、`verify()` は innerText 空を不合格 | ラウンド 2 の Low〜Medium 指摘 | 12 §2 / §5、schemas、14 §8、16 §4、10 §7、ADR-004 | 決定 |
| A-046 | 2026-09-14 | Decision | Phase 2 自己検証ラウンド 2 の 44 件（High 2・Medium 12・Low 30）をすべて反映。ラウンド 3 は行わず Phase 3 の Codex 独立レビューに委ねる | 収束傾向（Critical 1→0、High 10→2）。残るのは実画面確認（Phase 4）に依存する項目 | `reviews/phase2-self-review-round2.json`、`reviews/phase2-self-review.md` | 実施済み |
| A-047 | 2026-09-15 | Decision | Phase 3 Codex 独立レビュー（`gpt-5.6-terra`, high, read-only）の 7 指摘を裁定: F-01〜F-07 すべて Accept（F-02 / F-07 は修正付き）。代替案 3 件は Reject 2・Deferred 1 | `reviews/architecture-review-adjudication.md` | 10〜17、ADR、schemas、02、04、16 | 実施済み |
| A-048 | 2026-09-15 | Decision | プロファイルパス検証は `fs.realpath` の canonical 比較に加え、最終要素・祖先が symlink / junction なら fail closed で拒否（F-01） | NTFS junction で通常 `User Data` を指す迂回を防ぐ（CON-004） | FR-015、15 §5、ADR-002、AC-012 | 決定 |
| A-049 | 2026-09-15 | Decision | `CAPTURE` / `STOP_TRACE` / `INSPECT_UI_REPORT` / `UPDATE_MARKER` / `DELETE_MARKER` / `CLOSE_BROWSER` は best-effort effect。失敗は `result.json.warnings[]` に記録し `WRITE_RESULT` を妨げない（F-02） | 診断成果物の失敗で `result.json` を失うと呼び出し元が `submitted` を判定できない | 11 §1 / §4、12 §3.3、schema、AC-025 | 決定 |
| A-050 | 2026-09-15 | Decision | `observedPreset` の enum から `current` を除く。schema で `status` ごとの許容 `code` を制約（F-03, F-04） | 観測せずに `completed` を返す余地を契約から消す。「schema が正」を機械検証で担保 | 12 §3.3 / §3.4、schema、AC-007 / AC-015 | 決定 |
| A-051 | 2026-09-15 | Decision | `runtime/state/` は送信監査台帳として一般削除の対象外。`doctor` が欠落を警告（F-05） | 運用手順が R-003 の防御を無効化していた | 15 §7、17 §8、SEC-007、AC-002 | 決定 |
| A-052 | 2026-09-15 | Decision | `requestDir/result.json` 無しで `response.md` が残る場合は `INVALID_REQUEST(stale_response)` で停止し削除しない。requestDir は新規・空が呼び出し元の規約（F-06） | stale response の誤読を防ぐ | 10 §4、11 §4、12 §1、13、AC-006 | 決定 |
| A-053 | 2026-09-15 | Decision | `DISPATCH_SUBMIT` は click 直前に preset を再観測し、不一致なら click せず `SUBMIT_ABORTED(preset_changed)` → `MODEL_NOT_VERIFIABLE`、`submitted: no`、marker を best-effort 削除（F-07） | marker 書込〜click 間の preset 変化で異なる preset で送信される race | 11 §3 / §4 / §6、13、14 §5、FR-016(e)、AC-015 | 決定 |
| A-054 | 2026-09-15 | Decision | 設計文書 10〜17、ADR 001〜005、schemas を `FROZEN FOR MVP v1.0（2026-09-15）` とする。以降の変更は ADR または本ログに記録 | Phase 3 出口条件。Codex 指摘の Critical / High は未解決 0 | 各文書ヘッダ | 決定 |
| A-055 | 2026-09-15 | Decision | 初回ログインは **自動操作なしの通常 Chrome を専用プロファイルで起動**して人間が行う（`chrome.exe --user-data-dir=<runtime/profile>`）。`login` コマンドはこの手順を案内する。ブリッジは Google の自動操作ブラウザ拒否を回避しない | Phase 4 Live: Google ログインが Playwright 起動の Chrome を「安全でない」として拒否。手動 Chrome でのログインは同じプロファイルに保存され、Playwright 起動の Chrome でも有効（診断で確認） | ADR-002 補足、17 §2、`cli/main.ts` | 決定 |
| A-056 | 2026-09-15 | Decision | ログイン判定は「ログイン導線（`ログイン` / `Log in` ボタン）が見えれば `AUTH_REQUIRED`」。入力欄の有無だけでは判定しない | 未ログインの chatgpt.com にも入力欄が表示され、初回 `login` が誤って「ログイン済み」と判定した | 14 §3 `loginCta`、13 §4、`page.ts observeAuth` | 決定 |
| A-057 | 2026-09-15 | Decision | FR-024 の入力内容一致確認は NFKC + 全空白除去の空白非依存比較 | ProseMirror の段落改行で innerText が 2 文字ずれ `PROMPT_INPUT_FAILED` になった。目的は欠落検出であり整形差の検出ではない | `page.ts normalisePrompt`、AC-017 | 決定 |
| A-058 | 2026-09-15 | Decision | 実画面で確定した selector: 入力欄 `#prompt-textarea`、送信 `role=button name=メッセージを送信します`、停止 `data-testid=stop-button`、ターン `section[data-turn="assistant"]`、本文 `[data-message-author-role="assistant"] .markdown`、ターン単位コピー `data-testid=copy-turn-action-button`（「回答をコピーする」）、思考量メニュー `form [data-composer-transition-slot="trailing"] button[aria-haspopup="menu"]`。新規チャットは URL 遷移で開く。会話 URL は送信直後の一時 ID `/c/WEB:…` ではなく観測中の最新 `/c/<uuid>` を記録 | Phase 4 の実画面確認（OQ-002）。サイドバーの新規チャットリンクが複数あり unique にできない | `selectors.ts`（`verifiedOn: 2026-09-15`）、`controller.ts` | 決定 |
| A-059 | 2026-09-15 | Decision | DOM→Markdown 変換は ChatGPT の入れ子 `<pre>`（外側にヘッダの言語ラベルとコピーボタン、内側に `<pre><code>`）を外側単位で処理し、ヘッダ文字列を fence の言語にする。コピーボタンは scrollIntoView 後にクリックし、失敗時は force click | 言語ラベルが本文に混入し、fence に言語が付かなかった。応答アクションバーのボタンが通常クリックで 3 s タイムアウト | `markdown.ts`、`page.ts extractLatest` | 決定 |
| A-060 | 2026-09-15 | Decision | `verify()` は「本文トークンの被覆率 ≥ 0.85 かつトークン数比 0.5〜3.0」。UI 由来の語（コピーする / Copy / Code）は除外 | 先頭 200 文字の包含では innerText 側の UI ラベル（「TypeScript」）で正しい Markdown を棄却した | `verify.ts`、10 §7、AC-022 | 決定 |
| A-061 | 2026-09-15 | Decision | preset = 思考量スライダーの段階（5 段階、`aria-valuenow` 0..4）。段階 3 = 「極高」= `extra_high` のみ確定。モデルのラジオ（「最新」既定）は MVP では変更せず観測もしない。他段階のラベルと選択操作（矢印キー）は Phase 5 | OQ-002 / OQ-009。実画面のメニュー構造 | `selectors.ts PRESET_LABELS / EFFORT_SLIDER_INDEX`、14 §4、02 OQ | 決定（Phase 5 で拡張） |
| A-062 | 2026-09-15 | Decision | OQ-003 解決: 「回答をコピーする」は `navigator.clipboard.writeText` で Markdown を書く。ページ内フックで捕捉可能。`copy` を第 1 方式として維持 | LS-01 で `extractionMethod: copy`、fence 言語付き Markdown を取得 | ADR-004 維持、`docs/live-results/20260915-LS-01.md` | 決定 |
| A-063 | 2026-09-15 | Decision | Phase 4 では `preset: current` のみ受け付け、それ以外は `MODEL_NOT_VERIFIABLE`（`cause: preset selection is not implemented in Phase 4`）で送信しない。`--allow-unverified` は `verifiedOn` の無い候補を診断目的で有効にするフラグで、既定は検証済み候補のみ | 05 Phase 4 の範囲。fail closed を保ったまま段階的に実装 | `page.ts resolvePreset`、`cli/main.ts` | 暫定（Phase 5 で解除） |
| A-064 | 2026-09-15 | Decision | `result.json` の `conversationUrl` は origin + path のみ、`warnings[]` / `error.cause` / `error.message` は `redactSecrets` + 500 文字上限を通してから書く | Codex Phase 4 実装レビュー P4-1（High）。15 §4 / 13 §6 の契約を結果境界で強制する | `controller.ts sanitiseConversationUrl / sanitiseResultText` | 決定 |
| A-065 | 2026-09-15 | Decision | trace の `resources/*` は `.network` で MIME 許可された sha1 と `.trace` の `screencast-frame` が参照する sha1 のみ保持。拡張子による許可は廃止 | Codex Phase 4 実装レビュー P4-2（High）。15 §3 の許可リスト方式 | `trace-sanitizer.ts` | 決定 |
| A-066 | 2026-09-15 | Decision | PO の追加要望（モデル / effort 選択、リポジトリ読込、マルチモーダル、API 的利用、他 PJ オーケストレータ連携、コマンド資料）は `docs/21-CAPABILITY-EXPLORATION.md` に所見と検証計画としてまとめ、Phase 5 / 6 の範囲を再構成する。実装は各 Phase の続行指示後 | フェーズゲート（絶対条件 12）を維持しつつ要望に応える | `docs/20-COMMAND-REFERENCE.md`、`docs/21-CAPABILITY-EXPLORATION.md`、05 | 提案（PO 承認待ち） |
| A-067 | 2026-09-15 | PO | OQ-011 採用: `request.json` に `model` を追加（契約 1.1）。`preset` は effort 段階、`model` は `latest` / `gpt-5.6-sol` / `gpt-5.5` / `current` | PO 回答 1-A | 12、schemas、Phase 5 | 決定 |
| A-068 | 2026-09-15 | PO | OQ-012 採用: ファイル添付（`setInputFiles`）を使う。秘密らしいファイル（`.env`、鍵、`node_modules`、`.git` 等）は自動で拒否 | PO 回答 2-A | 21 §2、Phase 5 | 決定 |
| A-069 | 2026-09-15 | PO | OQ-010: 生成画像はダウンロードボタン経由を第 1 とし、**ページ内から画像データを直接取る（`fetch(img.src)` 等）ことも許容**。ただし ChatGPT の内部 API を呼ぶことは引き続き禁止（画像 URL はページ自身が表示のために読むリソースに限る） | PO 回答 3-B | 21 §3.3、15、Phase 6 | 決定 |
| A-070 | 2026-09-15 | PO | OQ-013: 他アプリからの利用は「自分の PC・自分のアカウント・バッチ処理」に限定。埋め込みチャットは目指さない | PO 回答 4-A | 21 §4、Phase 6 | 決定 |
| A-071 | 2026-09-15 | PO | Phase 1〜4 の成果をここでコミットする（ブランチ `phase-1/requirements`） | PO 回答 5-Now | Git | 決定 |
| A-072 | 2026-09-15 | PO | プランは **ChatGPT Pro**。利用上限（R-012）の目安はブリッジ経由で ChatGPT 自身に質問し、その回答を参考値として記録（公式情報ではない）。OQ-001（規約の手動確認）は引き続き PO 待ち | PO 回答 | 03 R-012、`docs/live-results/20260915-R-012-usage-limits.md` | 決定（参考値） |
| A-073 | 2026-09-15 | Decision | ブリッジが思考量スライダーを変更した場合、ブラウザ終了前（`CLOSE_BROWSER` の中、best-effort、15 s 上限）に元の段階へ戻す。失敗は `warnings[].restore_effort_failed` | 思考量はアカウントに永続化され、PO の通常利用の設定が変わってしまう。モデルのラジオはページ読込ごとにリセットされるので復元不要 | `page.ts restoreEffort`、`controller.ts CLOSE_BROWSER`、11 §4 補足 | 決定 |
| A-074 | 2026-09-15 | Decision | スライダー操作は `Home` → `ArrowRight` × index を **450 ms 間隔**で送る。間隔が短いと画面上は正しくてもサーバー保存が 1 段階遅れる（実測）。選択後はメニューを閉じて 1.2 s 待ち、トリガのラベルを読み戻して一致確認。不一致は `MODEL_NOT_VERIFIABLE` | 実画面の保存レース。fail closed | `selectors.ts EFFORT_KEY_INTERVAL_MS`、`page.ts selectEffort` | 決定 |
| A-075 | 2026-09-15 | Decision | トリガのラベルは既定モデルでは思考量のみ（「極高」）、モデル明示時や Pro では「<モデル略称> <思考量>」（「5.5 高」「6 Pro」）。末尾トークンを思考量として逆引きし、接頭部はモデルのヒントとしてログのみ | 実画面で確認 | `selectors.ts parseTriggerLabel` | 決定 |
| A-076 | 2026-09-15 | Decision | モデル選択はメニュー内 advanced view のラジオをクリックし、view が simple に戻るため再展開して `aria-checked` を確認。思考量はその後、**メニューを閉じて開き直してから**操作する（advanced view ではキーボードがスライダーに届かない） | 実画面で確認 | `page.ts resolvePreset` | 決定 |
| A-077 | 2026-09-15 | Decision | `data-message-model-slug` を `observedModelSlug` に記録し、`MODEL_SLUG_PATTERNS` / `EFFORT_SLUG_PATTERNS` と照合。不一致は `warnings[].model_slug_mismatch`（失敗にはしない。回答は取得済み）。観測値: latest+instant=`gpt-5-6`、latest+thinking=`gpt-5-6-thinking`、latest+pro=`gpt-6-pro`、gpt-5.5+high=`gpt-5-5-thinking` | 送信後の証跡。「最新 + Pro」は GPT-6 Pro にルーティングされ 50 件/週の枠を消費することが判明 | `selectors.ts slugMatches`、`controller.ts EXTRACT_LATEST` | 決定 |
| A-078 | 2026-09-15 | Decision | `model` の既定は `current`（観測のみ）。提案時は `latest` としたが、ラジオはページ読込ごとに「最新」へ戻るため `current` ≒ `latest` で、クリック操作が減る | A-067 の細部 | `schemas/request.schema.json` | 決定 |
| A-079 | 2026-09-15 | Decision | `usage [--json]` コマンド: `runtime/requests/*/result.json` の `submitted: yes` を slug パターン × 移動窓で集計し `runtime/limits.json`（初回に既定値を生成、PO が編集可）と比較。手動送信分は含まれない下限値であること、上限値が公式未確認であることを出力に明記 | PO 要望（クォータの目安を Claude / Codex から参照したい）。内部 API は使わない | `diagnostics/usage.ts`、20 §3 | 決定 |
| A-080 | 2026-09-15 | Decision | 添付は `request.json.attachments[]`（最大 20、1 ファイル 100 MB まで、相対パスは request.json 基準）。送信前に `contracts/attachments.ts` が拒否: 秘密らしい名前 / 拡張子（`.env`, `.pem`, `.key`, `id_rsa` 等）、`.git` / `node_modules` / `.ssh` 配下、2 MB 以下のテキストは `containsSecret` で内容走査、空ファイル、同名重複。違反は `INVALID_REQUEST`（exit 2、ファイル名のみ表示） | A-068 の実装。ファイルが PC 外へ出るので pre-browser で止める | `contracts/attachments.ts`、schema 1.1 | 決定 |
| A-081 | 2026-09-15 | Decision | 添付操作は `form input[type=file]#upload-files` への `setInputFiles`。チップ `form [role=group][aria-label]` の数がファイル数と一致し、送信ボタンの `aria-disabled` が解除されるまで待つ（上限 60 s + 15 s/MB）。サーバー側でファイル名が `name(1).ext` に変わることがあるため名前は照合せずログのみ。失敗は `PROMPT_INPUT_FAILED`（cause `attachment_failed: …`、送信前） | 実画面で確認（3 MB で 10 s 超） | `page.ts attachFiles`、14 §3 | 決定 |
| A-082 | 2026-09-15 | Decision | 新規チャットの入力欄に下書きが残っている場合は従来どおり fail closed（`composer_not_empty`）。ブリッジは下書きを消さない。運用手順に「ブラウザで入力欄を空にする」を追加 | ChatGPT は下書きをローカル保存する。診断スクリプトの残骸で再現 | 17 §5 | 決定 |
| A-083 | 2026-09-15 | Decision | 入力欄への直接入力は **20,000 文字まで**（`PROMPT_MAX_CHARS`）。超える request は送信前に `INVALID_REQUEST`（exit 2）で「長い本文は `attachments` へ」と案内 | 実測: `fill` は 10–20 k 文字で 10–15 s、57 k で 30 s 超。`paste` イベントなら 2 s だが ChatGPT 側が「貼り付けたテキスト」添付に変換し入力欄が空になるため本文照合ができない。添付経路なら 57 k 文字の全行を正確に参照できた（FL-01） | `contracts/request.ts`、12 §2、20 §4 | 決定 |
| A-084 | 2026-09-15 | Decision | `ENTER_PROMPT` のフェーズ上限（`PRESET_VERIFIED` 60 s）は添付がある場合 `uploadBudgetMs(合計バイト)`（60 s + 15 s/MB）だけ延長する | 3 MB のアップロードが 10 s 超で、固定 60 s では phase timeout になる | `controller.ts withPhaseLimit(extraMs)`、11 §5 補足 | 決定 |
| A-085 | 2026-09-15 | Decision | `bundle` は `--exclude` を glob 除外と `git diff` の pathspec 除外（`:(glob,exclude)`）の両方に適用。秘密パターンに当たるファイルが 1 つでもあれば全体を拒否（名前のみ表示） | 21 §2 / A-068 | `bundle/bundle.ts` | 決定 |
| A-086 | 2026-09-15 | Decision | `run --json` は result.json の内容 + `exitCode` + `resultPath` を標準出力に 1 行で出す。result.json が書かれない場合（`ALREADY_RUNNING` 等）は `status: not_started` のスタブ。`npm link` でグローバル `chatgpt-bridge` として他 PJ から呼べる（シンボリックリンク経由の起動判定を realpath で修正） | 21 §5b | `cli/main.ts` | 決定 |
| A-087 | 2026-09-15 | Decision | 添付ガードは `lstat` でシンボリックリンクを拒否し、内容走査は拡張子ではなくバイト判定（先頭 8 KB に NUL 無し = テキスト）で行う。テキストは 20 MB まで走査、超えるものは拒否 | Codex P5-1（High） | `contracts/attachments.ts` | 決定 |
| A-088 | 2026-09-15 | Decision | `bundle --diff` は `-` 始まりを拒否し、`git rev-parse --verify --end-of-options <ref>^{commit}` で解決した commit id のみを `git diff` に渡す | Codex P5-2（High） | `bundle/bundle.ts` | 決定 |
| A-089 | 2026-09-15 | Decision | トリガのラベル接頭部は `MODEL_HINTS`（"5.6"=GPT-5.6 Sol、"5.5"=GPT-5.5、"6"=最新の Pro 段階）のみ受理し、`hintMatches` でメニュー観測と突合。未知の接頭部・不一致は `MODEL_NOT_VERIFIABLE` | Codex P5-3。実画面で 3 種を確認 | `selectors.ts MODEL_HINTS / hintMatches`、14 §4 | 決定 |
| A-090 | 2026-09-15 | Decision | `limits.json` は `validateLimits` で全項目を検証し、不正なら警告して既定値を使う | Codex P5-4 | `diagnostics/usage.ts` | 決定 |
| A-091 | 2026-09-15 | Decision | 生成画像は抽出直後に best-effort で `requestDir/images/<n>.<ext>` へ保存し、`result.json.images[]`（契約 1.2）と `response.md` 末尾の `![image n](images/n.png)` に記録。画像のみのターン（本文なし）は `markdown: ""` / `dom` / `full` として成功扱い | MM-02 実画面: 画像は `img[alt^="生成された画像"]`（同一 src が 3 回描画、naturalWidth 1536）。ターンの「その他のアクション」にダウンロードは無く、画像クリックのビューア（role=dialog）内「保存」がダウンロード | `page.ts captureImages`、`controller.ts EXTRACT_LATEST`、schemas 1.2 | 決定 |
| A-092 | 2026-09-15 | Decision | 画像取得の主経路は **ページ内 `fetch(img.src)`**（A-069 で許容。ページ自身が表示に使う `chatgpt.com` の同一 URL のみ、52 ms、同一バイト列）。ビューア「保存」経由は `CHATGPT_BRIDGE_IMAGE_VIA_VIEWER=1` のときのみ | ビューア「保存」のダウンロード開始で Chrome stable が自動操作下でクラッシュ（`runtime/profile/Crashpad/reports` に 16:47 のダンプ 2 件、`BROWSER_CRASHED`）。診断で 3 回中 2 回再現 | `page.ts`、`config.ts` | 決定 |
| A-093 | 2026-09-15 | Decision | 送信後にクラッシュした request のマーカーは残り、同じ requestId の再実行は `SUBMIT_STATE_UNKNOWN` で止まる（設計どおり）。Phase 6 の実験で実動を確認 | 11 §4 | — | 確認 |
| A-094 | 2026-09-15 | Decision | `worker --queue <dir> [--once|--drain] [--poll-ms]`: `<dir>/pending/<requestId>/` を requestId 順に 1 件ずつ `running` へ移して `run` と同じ経路で処理し、exit 0 → `done`、1/2 → `failed`、3 → `blocked` **かつキュー全体を停止**、4（busy）→ `pending` に戻して再試行（3 回超で `failed`）。HTTP サーバも DB も持たない。SIGINT は現在の 1 件を終えてから停止 | 21 §5c。他 PJ / オーケストレータからの利用形態を「ディレクトリ規約 1 つ」に固定する | `cli/worker.ts`、20 §4f | 決定 |
| A-095 | 2026-09-15 | Decision | キュー項目の `result.json` 内の `responseFile` / `resultPath` は処理時点（`running/`）の絶対パスのまま。移動後は項目ディレクトリ基準で `response.md` / `result.json` / `images/` を読む。`usage --queue <dir>` で done / failed / blocked の分も集計 | ディレクトリ移動は atomic な `rename` で行い、result.json は書き換えない（FR-013） | 20 §4f | 決定 |
| A-096 | 2026-09-15 | Decision | 契約 1.2: `newChat: false` + `conversationUrl`（`https://chatgpt.com/c/<id>`、result.json から取る）で既存会話に追記できる。URL 遷移後にパスが `/c/<id>` のまま・assistant ターンが 1 件以上・生成中でない・入力欄が空、を確認して送信。満たさなければ `conversation_not_found` 等で送信せず停止。抽出は送信後の最新ターン（ベースラインは既存ターン数） | EX-03。Live で前メッセージの内容を正しく想起、存在しない id は fail closed | `page.ts openConversation`、schema `allOf`、12 §2 | 決定 |
| A-097 | 2026-09-15 | Decision | `worker` は起動時に `running/` の孤立項目を回復する（`result.json` の status で done / blocked / failed、無ければ `pending` へ）。終端への移動は 5 回リトライし、失敗したら worker を停止（exit 1） | Codex P6-2（High） | `cli/worker.ts recoverRunning / moveDir` | 決定 |
| A-098 | 2026-09-15 | Decision | `openConversation` は遷移後に origin と pathname の両方を確認する。`result.json.images[]` は `images/<n>.<ext>` 形式・重複なし・`completed` 限定を schema と invariant で強制 | Codex P6-1 / P6-3 | `page.ts`、`schemas/result.schema.json`、`invariants.ts` | 決定 |
| A-099 | 2026-09-15 | Decision | 思考量の復元は `WRITE_RESULT` の先頭で行う（1 回のみ、15 s 上限）。`CLOSE_BROWSER` では復元済みなら何もしない。これで `restore_effort_failed` が必ず result.json の `warnings` に載る | Codex 最終レビュー P7-1 | `controller.ts restoreEffortBestEffort`、11 §4 | 決定 |
| A-100 | 2026-09-15 | Decision | 画像取得は `AbortSignal` 付きで呼び、120 s で abort したうえで元の Promise の完了を待ってから result を確定する。abort 後は書き込まない | Codex 最終レビュー P7-2（成果物の確定境界） | `controller.ts`、`page.ts captureImages` | 決定 |
| A-101 | 2026-09-15 | Bugfix | 実戦投入初日、PixivVault オーケストレータ + PO 手動操作の並行利用で `login` 中のブラウザクラッシュ、`ALREADY_RUNNING`/`doctor` の判定不一致、`doctor` の login 診断が同一状況で毎回違う種類のエラー（timeout / AUTH_REQUIRED / net::ERR_ABORTED）になる、という報告。調査結果: (1) `checkProfileFree()` は実機検証で正しく動作（別プロセスの Chrome を検出）— 疑いは晴れた。(2) `S:` ドライブはローカル固定ディスク（`DriveType=3`、`ProviderName` 空）と確認 — ネットワーク共有ではない。(3) `lock.ts`/`doctor.ts` の stale 判定は同一の `judgeStale`/`defaultLockDeps` を共有しており、Windows の PID 再利用時刻チェック（PowerShell 経由）も実機で正常動作を確認 — ロジック自体にバグは無く、doctor と run の判定不一致は「2 回のチェックの間に lock ファイルの実体が変化した」（＝真の並行アクセス）で説明がつく。(4) `cmdLogin`/`doctor` の login probe は `RunController` を経由しないため FR-042 のリトライが効かず、`navigateAndObserveAuth()` の単発失敗（NOT_READY）がそのまま表面化していた — これが「毎回違うエラー」の主因と判断。(5) `cmdLogin` の `onCrash` はログ出力のみで、クラッシュ後もポーリングを継続し得た（非対話環境では `waitForEnter` の EOF で早期終了するため「クラッシュ直後に終了したように見える」）。**根本要因の最有力候補は、私（Claude）がグローバル `CLAUDE.md`/`AGENTS.md` で全セッションに chatgpt-bridge の利用を無条件に勧めたことで、単一専有リソースへの並行アクセスに関する注意喚起が無かったこと** | `cli/main.ts observeAuthWithRetry`（NOT_READY のみ最大 3 回・バックオフ付きで再試行。AUTH_REQUIRED/CHALLENGE/WRONG_PAGE は再試行しない＝fail closed を維持）、`withBrowser` に `CrashState` を追加し `login` がクラッシュ時に即座に明確なメッセージで exit 1（ポーリングを継続しない）、`doctor`/`inspect-ui` の login probe もクラッシュを検知、`SKILL.md`・グローバル指示ファイルに並行利用の注意を追記、`biome.json` に `tests/fixtures/**` の除外を追加（無関係な既存の lint エラーを修正） | 決定・修正済み（Codex Medium レビュー実施、A-102 参照） |
| A-102 | 2026-09-15 | Bugfix | A-101 の Codex レビュー: High 2 件（`observeAuthWithRetry` がクラッシュを考慮せず死んだページへ再試行し得る／`cmdLogin` のポーリングで `currentUrl()` の reject が `.catch()` の外で `poll` 全体を reject させ `withBrowser` の外まで伝播し得る）、Medium 3 件（`cmdLogin` で `AUTH_OK` 判定がクラッシュ判定より先に評価される不統一／`inspect-ui` がクラッシュ後も死んだページを操作し続ける／新経路に回帰テストが無い）。全て採用・修正 | `reviews/login-reliability-fix-codex.md` / `-adjudication.md` | `observeAuthWithRetry` に `CrashState` を渡し試行前後で確認、`currentUrl()`+`observeAuth()` を単一 try/catch に統合、3 箇所でクラッシュ判定を `auth.kind` 判定より先に、`inspect-ui` はクラッシュ時に即 `return 1`、`tests/unit/login-retry.test.ts` を追加（`ChatGptPort` の `Pick` 注入で純粋関数として単体テスト可能に）。`cmdLogin`/`doctor` 自体のポーリングループはなお実ブラウザ依存で未テスト（要リファクタ、未解決として明記） | 決定・修正済み・Unit 140 件通過 |
| A-103 | 2026-09-16 | Decision | PO 報告: 「起動のたびにブラウザを開閉しており、しばらくすると使えなくなり再ログインが必要」「Win/Mac それぞれで半恒久的にログインしておきたい」。PO からは絶対条件の見直しも含めて検討する許可があったが、**ステルス化・bot 検知回避は今回も行っていない**（禁止事項は維持）。代わりに任意（opt-in）の常駐ブラウザ daemon を追加し、`run`/`login`/`doctor`/`inspect-ui` が毎回ブラウザを起動・終了する代わりに daemon を使い回せるようにした。daemon は `--start-minimized` で起動するため画面を占有しない。検証で判明した事実: (1) 単発の自動化ブラウザでさっき確認した「ログイン済み」がその後の時間経過だけで（ブリッジの開閉操作なしに）ログアウト状態に戻っているのを実機で確認した（スクリーンショットで通常のログアウト画面と確認、Cloudflare 拒否ではない）。これは「開閉の繰り返しが bot 判定を招く」という仮説だけでは説明できず、**セッション自体に何らかの有効期限がある可能性**を示す一次証拠。daemon は開閉頻度を減らすので有効な改善だが、この件を完全には解決しない可能性がある。(2) daemon 経由でも `run`（本番の主経路、Google ログイン不要な通常フロー）が実際に送信・応答取得まで完走することをライブで確認した（`daemon-test-1`、16 秒で completed） | PO 依頼（実害あり）。絶対条件（ボット検知回避禁止）は維持しつつ、開閉頻度という別の切り口で緩和 | `browser/daemon.ts`（新規、pidfile+ポートで daemon 管理）、`browser/daemon-worker.ts`（新規、`--remote-debugging-port` 付きで `launchPersistentContext` する常駐ワーカー）、`browser/launch.ts`（`BrowserSession.attach()` 追加、`close()` は attach 時は検知のみでブラウザを閉じない）、`cli/adapters.ts`（`playwrightBrowser().checkProfileFree()`/`launch()` が daemon を透過的に優先。`run`/`worker`（`RunController`）と `login`/`doctor`/`inspect-ui`（`withBrowser`）の両方に効く）、`cli/main.ts`（`daemon start\|stop\|status` サブコマンド追加）、`diagnostics/doctor.ts`（`daemon` 項目追加。daemon 起動中は `profile.free`/`profile.processes` の「busy」を想定内として OK 扱いに変更） | 決定・実装済み・Unit 140 件通過・実機で `run`/`doctor` 完走確認。Mac 側は未検証（同じ `daemon start` が使えるはずだが要確認） |
| A-104 | 2026-09-16 | Bugfix | A-103 の Codex レビュー: High 3 件（固定・無認証の CDP ポートに他ローカルプロセスが相乗り／乗っ取りし得る、`stopDaemon` が PID 再利用や不正 PID を検証せず kill し得る、`daemon start`/`stop` がプロファイル安全境界と bridge 排他ロックを迂回する）、Medium 2 件（health/readiness 判定が実体確認なしで競合時に誤判定し得る、`stopDaemon` が kill 失敗でも `ok:true` を返し Chrome を孤児化し得る／`attach()` が CDP の `Browser` を保持せず `close()` で正しく disconnect していない）。固定ポート撤廃とプロファイル安全境界の統合、PID 再利用ガード、kill 実死確認は採用。CDP の完全な認証チャネル化（named pipe + ACL）と、同時起動時の nonce ハンドシェイクは、PO 専用 PC という前提でのコスト対効果を考え今回は見送り、残存リスクとして明記 | `reviews/daemon-mode-codex.md` / `-adjudication.md` | `browser/daemon.ts`（固定ポート `9876` → OS 割当のランダム空きポート、`checkDaemon`/`stopDaemon` に `lock.ts` と同じ PID 再利用ガード、`stopDaemon` は kill 後に実死を確認してから state を削除・成功を返す）、`browser/daemon-worker.ts`（state ファイルをアトミック書き込み＝tmp+rename に変更）、`browser/launch.ts`（`attach()` で `connectOverCDP()` の `Browser` を保持し、`close()` はそれを `browser.close()`（＝disconnect のみ、daemon 本体は終了しない）で明示的に切断）、`cli/main.ts`（`daemon start`/`stop` に `checkProfilePath` ガードと bridge lock の取得・解放を追加、実行中コマンドがあれば `ALREADY_RUNNING`）。**残存リスク（許容）**: CDP ポートは 127.0.0.1 バインドのみで無認証のまま — 同一 PC 上の別ローカルプロセスからの接続は技術的に防げない。PO 専用 PC（他ユーザーのログインが無い）という前提で許容 | 決定・修正済み・Unit 140 件通過・実機で `daemon stop→start`（ランダムポート確認）→`doctor` 3 回連続・`run` 完走・`daemon stop` の実死確認を確認 |
| A-105 | 2026-09-16 | Decision | PO 依頼で「拡張機能や定期実行で回避できないか調査」。まず反証実験: 「`--start-minimized` でウィンドウ最小化 → Chrome の背景タブタイマー間引きで `document.visibilityState` が `hidden` になりセッション自動更新が止まる」という仮説を実機で検証したところ**誤り**と判明（Playwright/CDP 制御下では最小化していても `visibilityState: "visible"`, `hasFocus: true` のまま — CDP が自動操作用にページをアクティブ扱いし続けるため）。ウィンドウを画面外へ動かす対策は不要と判断。Web 検索で確認した OpenAI コミュニティの情報では、ChatGPT はアイドルセッションを 15〜30 分程度で切ることがあり「開いたままにするなら 15〜30 分に一度は操作すること」が推奨されている。これに基づき、拡張機能ではなく **daemon 自身（Node プロセス、ブラウザのタイマー間引きの影響を受けない）が 15 分おきに `page.goto("https://chatgpt.com/")` する keep-alive を追加**。実行中の `run`/`login` と衝突しないよう、bridge のロックファイルが存在する間はスキップする（チェックと実行の間に小さな競合窓が残るが、単一ユーザー PC 前提で許容） | PO 依頼（実害の再発防止）。仮説を検証せず実装すると無意味な変更（画面外移動）をしてしまうところだった — 実機検証を優先 | `browser/daemon-worker.ts`（`--keepalive-ms` 引数、`--lock-path` 引数、`setInterval` での定期 `page.goto`、ロック保持中はスキップしログ）、`browser/daemon.ts`（`startDaemon` が `CHATGPT_BRIDGE_DAEMON_KEEPALIVE_MS` を worker に伝搬、`--lock-path` を自動算出して渡す） | 決定・実装済み・Unit 140 件通過・実機で 20 秒間隔に短縮して 3 周期の `reload` 成功を確認、`run` 実行中の 2 周期が `skipped: lock held` として正しくスキップされ `run` 自体も正常完走することを確認。長期（数時間〜）でセッションが本当に維持され続けるかは未検証（今後の運用で観察） |
| A-106 | 2026-09-16 | PO | PixivVault 側セッションからの要望（PO 経由で relay）: リポジトリ用の ChatGPT Project にチャットをまとめたい。選択肢は①`conversationUrl` 使い回し（会話が伸び続け文脈が汚れる）②ブリッジに Project 対応を追加、のうち PO が②を選択。実画面で調査: サイドバーの Project 行はクリックでは展開のみで遷移せず、ホバーで現れる鉛筆アイコン（`aria-label="プロジェクトのホームを開く"`）が `https://chatgpt.com/g/g-p-<hash>-<slug>/project` へ遷移させる。このページには通常と同じ `#prompt-textarea` の composer があり、そこから送信すると **その Project 内**に新規チャットが作られる（実機で「新機能テスト」チャットが PixivVault Project のチャット一覧先頭に現れることを確認）。ただし Project 内の会話 URL は `/c/<id>` ではなく **`/g/g-p-<hash>-<slug>/c/<id>`**（project の下にネスト）という別パターンであることも実機で判明。既存の `conversationUrl` 追跡（`openConversation` の正規表現、`controller.ts` の URL 捕捉、schema の pattern）は素の `/c/<id>` しか見ておらず、このままでは Project 内チャットの `conversationUrl` が正しく記録・再開できなかった（実機で再現し修正） | PO 依頼（relay）。実画面で selector・URL パターンを確認してから実装（推測実装はしない方針） | schema: `schemaVersion` に `1.3`、新規 `project`（`newChat:true` のときのみ有効、pattern `^https://chatgpt\.com/g/g-p-[A-Za-z0-9-]+/project$`）、`conversationUrl` の pattern を `/c/<id>` と `/g/g-p-<id>/c/<id>` の両方に対応。`contracts/types.ts` に `project?: string` と `schemaVersion: ... \| "1.3"`。`chatgpt/page.ts`: `openProject(url)`（`openConversation` と同じ fail-closed パターン：origin/pathname 不一致で `project_not_found`、生成中/未送信でない composer を確認）、`CONVERSATION_PATH_RE` を新設して `openConversation` とも共有。`state/machine.ts`: `NewChatFailure` に `project_not_found` を追加。`state/ports.ts`/`cli/adapters.ts`: `ChatGptPort.openProject` を追加・配線。`state/controller.ts`: `OPEN_NEW_CHAT` エフェクトで `newChat!==false && project` のとき `openProject` を使う分岐、会話 URL 捕捉も `CONVERSATION_PATH_RE` に統一 | 決定・実装済み・Unit 140 件通過・実機で新規 Project 内チャット作成と、そのネスト形式 `conversationUrl` での継続送信の両方を確認 |
| A-107 | 2026-09-16 | Bugfix | A-106 の Codex レビュー: High 1 件（`controller.ts` の会話 URL 捕捉を `CONVERSATION_PATH_RE` に統一した際、従来あった origin チェックが抜け落ち、外部 origin の同形パスを `conversationUrl` として保持し得る実回帰）、Medium 2 件（`project` の schemaVersion 限定という説明が実態と不一致／A-106 の受理・拒否条件を直接検証する単体テストが無い）。全て採用（Low の pattern 重複は実害無しと判断し見送り） | `reviews/projects-feature-codex.md` / `-adjudication.md` | `controller.ts` の会話 URL 捕捉に `origin === CHATGPT_ORIGIN` の明示チェックを再導入、`schemas/request.schema.json` の `project` 説明文を実態（schemaVersion は文書上の目安、enforcement なし）に合わせて修正、`tests/unit/contracts.test.ts`・`tests/unit/controller.test.ts` に A-106 の受理・拒否条件（project 単体/相互排他/外部 origin 拒否/nested conversationUrl 許可・外部 origin 非捕捉）のテストを追加 | 決定・修正済み・Unit 143 件通過 |
| A-108 | 2026-09-16 | Bugfix | Mac 側セッション（EMAKINOCO、`docs/live-results/20260916-macos-check.md`）の実機報告: このリポジトリを Win/Mac で同じ SMB 共有（`S:\` = `/Volumes/Share/...`）から直接使っており、`runtime/`（profile・`locks/bridge.lock`・`daemon.json`・`state`）が両 OS で共有されていた。実害: (1) Mac で `daemon start` すると Windows の `daemon.json` を上書きし Windows 側が自分の daemon を見失う、(2) Windows の `run` 中に Mac が `bridge.lock` を見ると、`process.kill(pid,0)` は他 OS の PID 空間を判定できないため「不在」と誤判定して stale 回収してしまい、single-flight が壊れる（実際に Windows 側 daemon の keepalive ログに Mac の `run` 中の `skipped: lock held` が記録され、ロック自体はホストをまたいで機能していたが、逆方向の stale 判定は安全ではなかった）、(3) Mac の Chrome は Cookie を Keychain 鍵で、Windows は DPAPI で暗号化するため、`scripts/manual-login.mjs` の手動 Chrome（フラグ無し）でログインしても Playwright 側の自動化 Chrome（既定で `--password-store=basic --use-mock-keychain` を付与）は復号できず `AUTH_REQUIRED` のまま。さらに調査中、**`node_modules` も同じ共有上にあり、Mac の `npm install` が Windows 用のネイティブバイナリ（rolldown の `binding-win32-x64-msvc`）を `binding-darwin-arm64` で上書きし、Windows 側のテストが全滅**しているのを発見・復旧した（`npm install` で再インストール）。これは `runtime/` の分離だけでは解決しない、リポジトリフォルダ自体をホスト間で共有すべきではないという、より根本的な問題 | Mac 側実機報告。「憶測ではなく実機で確認してから直す」の方針どおり、報告された実害を Windows 側でも再現・検証してから修正 | `cli/config.ts`（`CHATGPT_BRIDGE_RUNTIME_DIR` 環境変数を追加。未設定時は従来どおり `REPO_ROOT/runtime`）、`state/lock.ts`（`LockRecord`/`LockDeps` に `hostname` を追加。`judgeStale` は record の hostname が自ホストと異なる場合、PID 生死を判定せず無条件に「stale ではない」とする — 他ホストの PID は判定不能なため。hostname が空（A-108 以前の lock ファイル）は従来どおりの同一ホスト判定にフォールバック）、`browser/daemon.ts`/`daemon-worker.ts`（`DaemonState` に `hostname` を追加。`checkDaemon` が他ホストの daemon.json を `foreign: true` として区別し、`startDaemon`/`stopDaemon` は foreign な記録を一切上書き・kill しない）、`diagnostics/doctor.ts`（`runtime.location` 項目を新設し、パスがネットワークマウントらしい形（UNC、`/Volumes`、`/net`、`/mnt`）のとき warn。`profile.free`/`profile.processes`/`daemon` の表示に foreign ホストの場合の説明を追加）、`scripts/manual-login.mjs`・`browser/launch.ts`・`browser/daemon-worker.ts`（darwin のとき `--password-store=basic --use-mock-keychain` を明示的に付与し両者を一致させる。自動化 Chrome 側は Playwright の既定で既に付いていたが、明示化して意図を残す）、SKILL.md にリポジトリ共有マウント禁止と `CHATGPT_BRIDGE_RUNTIME_DIR` の案内を追記 | 決定・修正済み・Unit 149 件通過（`tests/unit/daemon.test.ts` 新規、`lock-marker.test.ts` に foreign-host テスト追加）。実機で偽の他ホスト daemon.json に対し `daemon start/stop/status` が全て正しく拒否し、本物の Windows daemon には触れないことを確認。node_modules の破損は npm install で復旧確認済み |
| A-109 | 2026-09-16 | Bugfix | A-108 の Codex レビュー（結論: Block 推奨）: High 4 件（単一共有 `daemon.json` の TOCTOU で他ホストの state を上書き・削除し得る、`cli/adapters.ts` が foreign daemon 検出時もローカル起動にフォールバックし得る＝最重要、hostname は認証情報でなく偽装で任意 PID を kill させ得る、worker の一時ファイル名がホスト間 PID 衝突を考慮していない）、Medium 4 件。設計を単一共有ファイルから **ホストごとに独立したファイル**（`daemon.<hostname>.json`）へ変更し、TOCTOU・一時ファイル名衝突は「検知して拒否」ではなく「そもそも他ホストのファイルに書けない」構造に変更して解消。`cli/adapters.ts` の foreign 時フォールバックは最重要 High として修正し、実機で Windows daemon 停止中に偽の Mac daemon ファイルのみが存在する状態を作り、`run`/`doctor` が正しくローカルブラウザを起動せず `PROFILE_IN_USE` で止まることを確認した。hostname 偽装・DoS の 2 件は、この機能が前提とする「PO 本人の複数マシン」という脅威モデル（A-103/A-104 の CDP ポート無認証と同じ前提）の範囲内として許容し見送り。`runtime.location` の Windows マップ済みドライブ検知漏れは `Win32_LogicalDisk` 判定を追加して解消 | `reviews/cross-host-fix-codex.md` / `-adjudication.md` | `browser/daemon.ts`（全面書き換え: `daemon.<hostname>.json`、`findForeignDaemon()` はディレクトリスキャンのみで書き込みなし）、`cli/adapters.ts`（`checkProfileFree()`/`launch()` が foreign を busy として扱う）、`diagnostics/doctor.ts`（`isWindowsMappedNetworkDrive()` 追加、daemon 項目の文言修正）、`tests/unit/daemon.test.ts`（全面書き換え）、`tests/unit/adapters.test.ts`（新規、Q-2 の直接検証） | 決定・修正済み・Unit 152 件通過・実機で foreign daemon 検出時の `run`/`doctor` 拒否を確認。**このリポジトリ自身の移行**: 旧形式の共有 `runtime/daemon.json`（hostname 無し）を手動で停止・削除し、新形式で daemon を再起動済み。他ホスト（Mac）で同様の移行が必要 |
| A-110 | 2026-09-17 | Bugfix | Mac 側セッション（emakinoco-cd）からの追加報告: daemon が追跡している単一の `page` だけが死ぬ（macOS がバックグラウンドタブをメモリ圧迫で回収したと推測）一方 `context` 自体は生きたままのケースで、`context.on("close")` が発火せず daemon が「running」のまま 13 時間 keepalive 失敗し続けた。この間 `run`/`doctor` は `PROFILE_IN_USE`→新規起動という綺麗な経路ではなく、`INVALID_STATE`（`Frame has been detached`）や `login: NOT_READY` という分かりにくいエラーで止まっていた。加えて前日、生成中に `page closed` で `BROWSER_CRASHED` になった事例が 1 件（該当リクエストは submitted:yes、以降のキュー項目は正常完了）。提案されたとおり (1) daemon 自身が keepalive 失敗時にページを再取得し、それでも連続で失敗したら自己終了して `doctor`/`run` が「daemon 無し」を正しく報告できるようにする (2) `attach()` 側でも死んだページを即座に検知して張り替える、の両方を実装した | Mac 側実機報告（13 時間再現データ付き）。ページ単位の死は「実機で見つけてから直す」の対象として妥当と判断 | `browser/daemon-worker.ts`（`keepAliveTick` が `page.isClosed()` を先にチェックし、`goto` 失敗時は `context.newPage()` で即時リカバリ。それも連続 2 回失敗したら `shutdown()` して daemon.json を削除・プロセス終了）、`browser/launch.ts`（`attach()` に `getUsablePage()` を追加 — `isClosed()` に加え軽い `evaluate()` で実際に応答するか確認し、ダメなら `context.newPage()`。それも失敗すれば `attach()` 全体が `{ok:false}` を返し、既存の「fresh launch へフォールバック」経路（A-108 で強化済み）がそのまま効く） | 決定・修正済み・Unit 152 件通過。実機で「context は生きたまま tracked page だけ close」を再現し、`doctor`/`run` が daemon 再起動を待たず `getUsablePage()` で即座に復旧することを確認（daemon の pid は変わらず）。連続失敗による daemon 自己終了は経路のみコードレビューで確認（安全に模擬する手段が無く実機未検証、Mac 側での長時間再現待ち） |
| A-111 | 2026-09-17 | Bugfix | A-110 の Codex レビュー（結論: Block 推奨）: High 4 件（keepalive のロック確認が1回のみで実行中クライアントのページを daemon が閉じ得る、`newPage()` 成功だけで壊れたページを「復旧成功」とみなしていた、`evaluate()` にタイムアウトが無く無期限にハングし得る、`shutdown()` が `context.close()` より先に state ファイルを消すため実際は残っている daemon を「不在」と誤報し得る）、Medium 3 件（壊れたページの放置でタブが増える、`setInterval` の多重実行防止が無い、`attach()` 失敗時に CDP 接続をリークする）。全て採用 | `reviews/daemon-selfheal-codex.md` / `-adjudication.md` | `browser/timeout.ts`（新規、共通の `withTimeout()` ヘルパー）、`browser/daemon-worker.ts`（`goto()`/`evaluate()`/`context.close()` に bounded timeout、破壊的操作の直前にロックを再確認して held なら中断、`tickInFlight` で多重実行防止、`shutdown()` は close→unlink の順に変更）、`browser/launch.ts`（`getUsablePage()` が新規ページも検証してから返す、壊れた候補ページは再利用前に close、`attach()` 失敗時は確立済み CDP 接続を必ず close） | 決定・修正済み・Unit 152 件通過。実機で「tracked page だけ close、context は生存」を再現し、reworked 後も daemon 再起動を待たず即座に復旧すること・実際の `run` が正常完走することを再確認。連続失敗時の自己終了・ロック競合時の中断はコードレビューで経路確認のみ（実機での長時間再現は Mac 側フォローアップ待ち） |
| A-112 | 2026-09-17 | Bugfix | PixivVault オーケストレータが `#124`（Pro 生成が `GENERATION_TIMEOUT` として誤って「失敗」判定され、実際はまだ生成中の回答に対して新規チャットで再送し、専有プロファイル内で生成が並走しかけた事故）を受け、無許可でこのリポジトリに直接コミットした（手続き上の問題は PixivVault 側 `docs/INCIDENT_LOG.md` `#125` に別途記録済み、こちらでは扱わない）。技術内容の独立レビューを依頼されたので、自己申告を鵜呑みにせず実際の diff・テスト・schema を検証した。`judge()`（`completion.ts`）がタイムアウト到達時に `streaming` を見て `VERDICT_TIMEOUT_ACTIVE`／`GENERATION_TIMEOUT_ACTIVE` を分岐する設計・実装・テスト（`completion.test.ts`/`machine.test.ts`/`controller.test.ts`/`error-codes.test.ts`）は妥当で、`10-ARCHITECTURE.md` §6 の「上から順に評価」というルール順序も変えていない。**ただし `schemas/result.schema.json` の `errorCode` enum に `GENERATION_TIMEOUT_ACTIVE` が抜けており、実際に発火すると `checkResultInvariants` が拒否して `WRITE_FAILED (result.json)` になり、result.json が一切書かれない（この修正が対処しようとしていた事故そのものより悪い結果）ことを実機再現で確認**。ユニットテストは `writeResult` を fake しているため検出できていなかった | PixivVault オーケストレータからの独立レビュー依頼。実機で再現してから判断する方針どおり | `schemas/result.schema.json`（`GENERATION_TIMEOUT_ACTIVE` を enum に追加、blocking bug の修正）、`docs/10-ARCHITECTURE.md`・`docs/11-STATE-MACHINE.md`・`docs/13-ERROR-MODEL.md`・`docs/17-OPERATIONS.md`（新コードの記載漏れを追記）、`tests/unit/contracts.test.ts`（`ERROR_CODES` の全メンバーが `schemas/result.schema.json` と同期していることを保証する再発防止テストを追加。schema 修正を戻すと実際に落ちることを確認済み） | 決定・修正済み・Unit 156 件通過。コミット自体（`565e9d0`/`418a710`）は未 push のローカルコミットのままキープ（revert 不要、技術的に妥当と判断）。この bugfix コミットは別途追加する |
| A-113 | 2026-09-18 | Bugfix | PO の依頼で、ブリッヂ自身での自己レビュー（ChatGPT Pro）に加え、AGY（Google Antigravity, Gemini系）に独立したセカンドオピニオンを、Jules（Google の非同期コーディングエージェント）にテスト追加を依頼した。AGY のレビューで実害あるバグ2件を発見: (1) `src/state/controller.ts` の `WRITE_RESULT` が、`result` が契約違反（例: A-112 のような enum 登録漏れ）で例外を投げると stderr に出すだけで **result.json を一切書かずに終わる**。A-112 は個別のケースを直したが、この構造的な穴自体は残っていた。(2) `src/browser/daemon.ts` の `findForeignDaemon()` が `state.profileDir === cfg.profileDir` という絶対パス文字列比較をしており、Windows(`S:\...`)とMac(`/Volumes/...`)で同じ共有ドライブでも文字列が一致しないため、**A-108 のクロスホスト排他制御が実質的に機能していなかった**（このリポジトリ自身の実機テストは、たまたま偽ファイルに同じ Windows パス文字列を使っていたため見逃していた） | AGY の独立レビュー（`runtime/self-review/agy-output.md`）。実機再現してから修正する方針どおり、両方とも実際にコードを読んで検証してから対応 | `src/state/controller.ts`（`WRITE_RESULT` の catch で、通常の書き込みが失敗したら `emergencyResult()`（必ずスキーマを満たす最小限のフォールバック、`status:failed`・`error.code:INTERNAL_ERROR`、`phase` は `submitted` から決定的に導出）で1回だけ再試行する）、`src/browser/daemon.ts`（`findForeignDaemon()` から `profileDir` 比較を撤廃し、`cfg.runtimeDir` 内に他ホストの `daemon.*.json` が存在すること自体を証拠として扱う） | 決定・修正済み・実機でクロスホスト検知の復旧を確認（Windows パスと Mac 風パスが混在する状況を模擬）。Codex に追加レビューを依頼（→ A-114 参照） |
| A-114 | 2026-09-18 | Bugfix | A-113 の Codex レビュー（結論: 承認不可）: 重大1件（`emergencyResult()` 自身も `error.cause` を500文字上限で構築しており、schema の実際の上限200文字を超えると再試行も同じ理由で失敗し得た）、High 2件（`emergencyResult()` が `original.warnings` を無検証でコピーしており、空文字列などスキーマ違反の温床になり得た／fallback 書き込み成功後も `this.result` が元の不正オブジェクトのままで、`run --json` の stdout とディスク上の result.json の内容が食い違い得た）、Medium 1件（A-113 の `profileDir` 比較撤廃は fail-closed だが、`CHATGPT_BRIDGE_PROFILE_DIR` を独立指定して同一 `runtimeDir` を共有する正当な別プロファイル運用まで誤って停止させ得る）。全て採用 | `reviews/agy-followup-codex.md` | `src/state/controller.ts`（`error.cause` 専用に200文字上限の `sanitiseErrorCause()` を新設し `emergencyResult()` とビルド通常経路の両方に適用、`emergencyResult()` は `original.warnings` を捨てて独自の1件のみに、fallback 書き込み成功後は `this.result` もフォールバック自身に統一）、`src/browser/daemon.ts`（`getOrCreateProfileId()` を新設 — プロファイルディレクトリ内に置く小さな UUID ファイルで、OS/マウントポイントに依存しない安定した「プロファイルの正体」を持たせる。`findForeignDaemon()`・`startDaemon()`・`daemon-worker.ts` を `profileDir` 文字列ではなく `profileId` で比較するよう統一） | 決定・修正済み・Unit 159 件通過（`profileId` を使った新規テスト2件を含む）。実機でのクロスホスト・別プロファイル共存の完全な実地検証は次回 daemon 再起動時に実施予定（このリクエスト実行中の pro レビューのロック解放待ち） |
| A-115 | 2026-09-18 | Decision | PO の依頼で ChatGPT Pro に2件の独立レビューを依頼した。(1) fail-closedの一貫性を問う自己レビュー(`reviews/chatgpt-pro-self-review.md`) — 15件の指摘中13件を別セッションのgeneral-purpose agentが実コード照合でCONFIRMED、1件PARTIALLY-TRUE、1件(daemon shutdown順序)は既にA-110で修正済みで指摘が古いことが判明。(2) 「1呼び出し元セッションから複数ChatGPTチャットを並列実行したい／複数マシン対応したい」というPOの新要求を踏まえた再設計ギャップ分析(`reviews/chatgpt-pro-redesign-review.md`) — 補助検証(フェイクポート再現)を6件実施し、**observeLoop相当がconversationUrlを無条件上書きし、別会話の回答を依頼の完了として保存しうることを実際にCOMPLETEDまで進めて実証**（fail-closedの最重要要件そのものが現状破れている）。結論: 全面書き直しは不要、既存の状態機械・ports構成の上にジョブ管理層を追加する方針。「bridge.lockを外して並列起動するだけ」「timeoutを伸ばすだけ」「SQLiteを共有ドライブに直接置くだけ」は明示的に不採用と裁定 | `reviews/chatgpt-pro-self-review.md`、`reviews/chatgpt-pro-redesign-review.md` | 2つのレビューの確認済み指摘・危険箇所を統合し、`docs/23-DURABLE-BRIDGE-PHASES.md`（Phase 0〜4の段階移行計画）を新設。Phase 0は既存の危険窓の縮小（17項目、durable化より前に完了させる）、Phase 1以降は単一マシン並列数1のdurable化→送信後回復→同一マシン並列化→複数マシン配送強化の順 | 決定・計画書作成済み。実装は別セッションのCodex(`gpt-5.6-terra`)へフェーズ単位で順次委譲する方針（未着手）。テスト・コードレビューの担当モデル/エフォートは別途使用量・タスクを見て検討 |
| A-116 | 2026-09-18 | Bugfix | Phase 0(`docs/23-DURABLE-BRIDGE-PHASES.md` 0-A、Codex quota残量の都合でこのセッション自身が実装)の最優先項目に着手。`observeLoop()`(`controller.ts`)は送信直後の`conversationUrl`を毎ティック無条件で現在のURLへ上書きしており、別の会話へPageが遷移してもassistant件数が偶然baseline+1に一致すれば、その会話の回答を依頼の`completed`として保存し得た（再設計レビュー§3.1でフェイクポートにより実証済みのバグ）。修正前に`git stash`でfix一式を一時的に外し、追加した再現テスト(`A-116`)が実際に`status:"completed"`のまま通ってしまう（＝バグを再現する）ことを確認してから、`stash pop`で戻して修正を検証した | `reviews/chatgpt-pro-redesign-review.md` §3.1、§6.2 | `controller.ts`（`conversationUrlLocked`を追加。送信直後の最初の一致観測で1回だけ`conversationUrl`を確定させ、以降は現在URLと不一致なら`VERDICT_CONVERSATION_MISMATCH`を`judge()`を経由せず直接発行してfail-closedに終端する）、`machine.ts`（`VERDICT_CONVERSATION_MISMATCH`イベントと`CONVERSATION_MISMATCH`エラーコードへの遷移を追加）、`contracts/types.ts`・`schemas/result.schema.json`（enum同期）、`docs/11-STATE-MACHINE.md`・`docs/13-ERROR-MODEL.md`・`docs/17-OPERATIONS.md`（新コードの記載追加）、`tests/unit/error-codes.test.ts`・`tests/unit/controller.test.ts`（レビューが実証した具体的なシナリオを再現する回帰テストを追加。fix無しで実際に落ちることを確認済み） | 決定・修正済み・Unit 161件通過（typecheck/lint/build clean）。`docs/23-DURABLE-BRIDGE-PHASES.md`のPhase 0残り16項目は別途進める |
| A-117 | 2026-09-18 | Bugfix | Phase 0-B-1(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`worker.ts`の`recoverRunning()`は`running/<id>`を所有者確認なしに`pending`へ戻していたため、同じqueueディレクトリへ2つ目の`worker`プロセスを起動すると、1つ目が処理中のjobをstartup時のrecoveryで誤って回収し得た（再設計レビュー§3.2でフェイクポートにより再現済み） | `reviews/chatgpt-pro-redesign-review.md` §3.2 | `worker.ts`（`processOne()`が`running/<id>/.worker-owner.json`にpid/startedAt/hostnameを書き込むようにし、`recoverRunning()`は`state/lock.ts`の`judgeStale()`と同じ生存確認パターン（別ホストは検証不能なので生存扱い、PID再利用はstartedAt比較で検出）でowner fileを確認し、生きているownerがいればそのentryに触れず残す。owner file書き込み失敗はbest-effortで無視（従来の挙動に単に戻るだけ））、`tests/unit/worker.test.ts`（owner生存/死亡それぞれのケースの回帰テストを追加） | 決定・修正済み・Unit 163件通過（typecheck/lint/build clean） |
| A-118 | 2026-09-18 | Bugfix | Phase 0-B-2(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`getOrCreateProfileId()`は読み取り・新規作成のいずれにも失敗した場合、非永続なランダムIDを返すfail-openな実装だった。呼び出すたびに異なるIDが返るため、A-113/A-114で導入したprofileId比較によるクロスホスト・別プロファイル識別が意味を失い得た（再設計レビュー§3.7で実証済み）。回帰テストは当初idPathの親を「ディレクトリの代わりにファイル」にする形で書いたが、それは`mkdir()`（元コードでも無防備）で即座に例外化するため新旧の差を検出できないことに気づき、idPath自体をディレクトリで塞ぐ（`mkdir()`は成功、`writeFile`/再読み取りがEISDIRで失敗する）形に修正して初めて、修正前コードが実際にfallback IDを返してしまうことを確認できた | `reviews/chatgpt-pro-redesign-review.md` §3.7 | `src/browser/daemon.ts`（全滅時はErrorをthrowするよう変更。呼び出し元（`findForeignDaemon`・`startDaemon`）は明示的なtry/catchを追加していない — `RunController.run()`の`try/catch`（controller.ts:150-162）が効果層からの例外を`INTERNAL_ERROR`として安全にresult.json化する既存の安全網、および`main.ts`末尾の`main().then(...,err=>...)`が`daemon start`単体実行時の同種の安全網であることを確認済みなので、素直にthrowさせるだけで良い）、`tests/unit/daemon.test.ts`（fail-closedへの回帰テスト2件） | 決定・修正済み・Unit 165件通過（typecheck/lint/build clean） |
| A-119 | 2026-09-18 | Bugfix | Phase 0-B-3(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`lock.ts`のstale lock reclaim中、判定した内容と実際にrenameで移動した内容が食い違った場合（＝reclaim中に別プロセスの生きたlockを誤って奪った場合）、`rename(stalePath, this.path)`で無条件に復元していたため、その復元の一瞬の隙に4人目のプロセスが正当に取得した新しいlockをrenameが問答無用で上書きし得た（再設計レビュー指摘）。実際に再現するテストを書くにあたり、rename前後の競合ウィンドウへ確定的に介入する手段が無かったため、`LockDeps`に`afterStaleRename`という最小のテスト用フックを新設（`isProcessAlive`/`processStartedAt`と同じ既存パターンを踏襲）し、4プロセスの競合を決定的に再現した上で、修正前のコードが実際にD（4人目）のlockをC（誤って奪われた3人目）のlockで上書きしてしまうことを確認してから修正した | `reviews/chatgpt-pro-redesign-review.md` §3(「stale lock recovery に所有権競合」) | `src/state/lock.ts`（復元を`rename()`ではなく`createExclusive()`（`wx`フラグでの排他作成）で行うよう変更。既に何かが`this.path`を占有していれば復元は失敗し、C側の内容はstalePathに残したまま`busy`を返す — doctorでの後始末対象にはなるが、生きているロックを破壊しない）、`LockDeps.afterStaleRename`（テスト専用フック追加）、`tests/unit/lock-marker.test.ts`（4プロセス競合の回帰テスト。fix無しで実際に落ちることを確認済み） | 決定・修正済み・Unit 166件通過（typecheck/lint/build clean） |
| A-120 | 2026-09-18 | Bugfix | Phase 0-B-4(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`daemon-worker.ts`のkeepaliveはロックファイルの「存在確認」(`access()`)のみで、実際にロックを取得していなかった（コード自身のコメントも「full mutual exclusion would need the daemon to participate in the lock protocol itself」と競合窓の残存を認めていた）。`daemon-worker.ts`にはユニットテストのハーネスが元々存在しない（実ブラウザを起動するtop-level-awaitスクリプトのため）ため、この修正はtypecheck/lint/build/既存166件のunit testの通過と、コードレビュー的な確認のみで検証している（ライブ再現テストは追加していない） | `reviews/chatgpt-pro-redesign-review.md` §3.6 | `src/browser/daemon-worker.ts`（`isLockHeld()`（存在確認のみ）を廃止し、`state/lock.ts`の`ProcessLock`を使って`keepAliveTick()`がpage操作の間ずっと本物のロックを保持するよう変更。2回目の`isLockHeld()`再チェックも、真の排他制御になったことで不要になったため削除） | 決定・修正済み・Unit 166件通過（typecheck/lint/build clean）。ライブ再現テストは未実施（daemon-worker.tsに既存のテストハーネスが無いため） |
| A-121 | 2026-09-18 | Bugfix | Phase 0-B-5(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`BrowserSession.close()`は「bounded close, then kill」というコメントに反し、`ctx.tracing.stop()`が完全に無期限、timeout後のfallback `ctx.close()`も無期限のawaitだった。`launch.ts`にもユニットテストハーネスが存在しない（実ブラウザ起動のため）ため、typecheck/lint/build/既存166件のunit testの通過のみで検証 | `reviews/chatgpt-pro-redesign-review.md` §3.6 | `src/browser/launch.ts`（既存の`withTimeout()`ユーティリティ（`browser/timeout.ts`、A-110で導入済み）を`tracing.stop()`とfallbackの`ctx.close()`双方に適用。Persistent contextにはプロセスハンドルが無く本当のkillはできないため、達成できる保証は「`close()`自体は必ず期限内に返る」こと（裏側のPlaywright呼び出しがハングしたままバックグラウンドに残る可能性は残る）） | 決定・修正済み・Unit 166件通過（typecheck/lint/build clean）。ライブ再現テストは未実施（launch.tsに既存のテストハーネスが無いため） |
| A-122 | 2026-09-18 | Bugfix | Phase 0-B-6(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`state/lock.ts`の`processStartedAt()`はmacOS/Linuxで常にnullを返し、`judgeStale()`（bridge.lockのstale判定）・`daemon.ts`の`verifyOwnedProcess()`（daemon PIDの所有権確認）の双方がPID再利用を検出できずbest-effortの生存確認のみに頼っていた。この開発機はWindowsのためmacOS/Linux分岐は実機検証できず、typecheck/lint/build/既存166件のunit testの通過のみで検証（既存のpid再利用検出テストはprocessStartedAtをfakeで差し替えているため実装変更の影響を受けない） | `reviews/chatgpt-pro-redesign-review.md` §3(「daemon PID所有権確認がMacで弱い」) | `src/state/lock.ts`（`defaultLockDeps.processStartedAt`にmacOS/Linux向けの`ps -o lstart= -p <pid>`分岐を追加。WindowsのWMI経由と同じ「記録した開始時刻より後に生成されていたらPID再利用とみなす」ガードが両OSで効くようになった）、`src/browser/daemon.ts`（コメント更新のみ、ロジックは`defaultLockDeps`を再利用しているため変更不要） | 決定・修正済み・Unit 166件通過（typecheck/lint/build clean）。macOS/Linux実機での検証は未実施（開発機がWindowsのため） |
| A-123 | 2026-09-18 | Bugfix | Phase 0-C-2(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`selectors.ts`の`resolve()`/`probe()`は`countVisible(loc)===1`を確認していながら、返す値は`loc.first()`（DOM順で最初の一致要素）だった。1件目がhidden、2件目がvisibleという構成では、visibility確認済みと思い込んだままhidden要素を操作対象として返し得た | `reviews/chatgpt-pro-self-review.md` #2 | `src/chatgpt/selectors.ts`（`countVisible()`とは別に、実際に可視と確認した`nth(i)`そのものを返す`findVisible()`を新設し、`resolve()`/`probe()`をそちらへ切り替え。`exists()`は真偽値のみ返すため対象外）、`tests/unit/selectors.test.ts`（1件目hidden・2件目visibleのfakeロケータで回帰テスト。fix無しで実際に落ちることを確認済み） | 決定・修正済み・Unit 167件通過（typecheck/lint/build clean） |
| A-124 | 2026-09-18 | Bugfix | Phase 0-C-3(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`page.ts`の`saveImageViaFetch()`はAbortSignalを受け取るが、page内`fetch()`へは渡っておらず（Playwrightのevaluate()はstructured-clonableな引数しか受け付けないためAbortSignal自体をCDP境界越しに渡せない）、abort確認も`evaluate()`が返った後だけだった。ハング時に呼び出し側が待ち続ける問題があった。`page.ts`にもユニットテストハーネスが無いため（実Playwright Page依存）、typecheck/lint/build/既存167件のunit testの通過のみで検証 | `reviews/chatgpt-pro-self-review.md` #10 | `src/chatgpt/page.ts`（`evaluate()`呼び出しを`withTimeout()`（60秒）と、signalのabortイベントを監視するPromiseの`Promise.race()`で包む。page内fetchそのものは引き続きキャンセルできない（CDP境界の制約は解消不能）が、この関数自身の待ち時間は確実に有界になる。abort後に元のevaluate()が遅れて解決/拒否してもunhandled rejectionにならないよう`.catch(()=>undefined)`を付与） | 決定・修正済み・Unit 167件通過（typecheck/lint/build clean）。ライブ再現テストは未実施（page.tsに既存のテストハーネスが無いため） |
| A-125 | 2026-09-18 | Bugfix | Phase 0-C-4(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`attachments.ts`のsecret scanは「2MiB以下、または既知のテキスト拡張子」の場合しか内容を読まなかった。2MiB超かつ非テキスト拡張子（例: `secret.pdf`/`notes.dat`）のファイルは一切内容検査されずに通過しており、「内容はバイトで判定する、拡張子ではない」というコード自身のコメントの約束と矛盾していた | `reviews/chatgpt-pro-self-review.md` #14 | `src/contracts/attachments.ts`（サイズ・拡張子に関わらず常に先頭8000バイトだけを読んで`looksText`を判定する`readHead()`を新設。text-likeと判定された場合のみ`LARGE_TEXT_SCAN_MAX_BYTES`（20MiB）を超えれば拒否、以下ならフル読み込みしてsecret scan。もはや拡張子で判定しないため`TEXT_EXT`定数と`CONTENT_SCAN_MAX_BYTES`定数を削除）、`tests/unit/attachments.test.ts`（大きい`.dat`ファイルへのsecret混入と、大きい正当なバイナリファイルの両方について回帰テスト。fix無しで前者が実際に通過してしまうことを確認済み） | 決定・修正済み・Unit 169件通過（typecheck/lint/build clean） |
| A-126 | 2026-09-18 | Bugfix | Phase 0-C-5(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`trace-sanitizer.ts`は`.network`/`resources/*`/既知のテキスト拡張子(`.trace`/`.stacks`)のいずれでもないエントリを無条件でそのまま通しており、将来Playwrightのtrace zip形式が新しいエントリ種別を追加した場合、secretを含んだまま通過し得た | `reviews/chatgpt-pro-self-review.md` #15 | `src/browser/trace-sanitizer.ts`（未知エントリも、先頭バイトでtext-likeと判定できれば行単位で`redactSecrets()`を適用してから書き出す。binary-likeなものは既存方針どおりそのまま通す — A-125の添付ファイルscanと同じ「拡張子でなく内容で判定」パターン）、`docs/15-SECURITY-AND-PRIVACY.md` §3手順6の記載更新、`tests/unit/security.test.ts`（未知のtext-likeエントリとbinary-likeエントリ双方の回帰テストを追加。fix無しで前者のsecretが実際に残ることを確認済み） | 決定・修正済み・Unit 169件通過（typecheck/lint/build clean） |
| A-127 | 2026-09-18 | Bugfix | Phase 0-C-1(`docs/23-DURABLE-BRIDGE-PHASES.md`)、Phase 0の最後の項目。`continueButton`（続きを生成＝truncated検出）・`sidePanel`（Canvas検出）・`challengeFrame`（CAPTCHA検出）のselector候補に`verifiedOn`が無く、既定の`verifiedOnly: true`では全候補がスキップされて`exists()`が常にfalseを返していた（＝これら3つの安全監視が通常運用で常に無効化されていた）。実画面での検証ができない状況で`verifiedOn`日付を捏造するのは不誠実なため、その代わりに「クリック/操作対象ではなく検出専用」という性質を利用し、これら3キーの`exists()`呼び出しだけは`verifiedOnly`設定に関わらず常に候補を試す方針にした（誤検知しても安全側に倒れるだけで、誤ってクリックする心配が無いため） | `reviews/chatgpt-pro-self-review.md` #1 | `src/chatgpt/page.ts`（`ChatGptPage`に`safetyCheckOpts`（`{verifiedOnly:false}`固定）を追加し、`challengeFrame`/`continueButton`/`sidePanel`の5箇所の`exists()`呼び出しを`this.sel`から`this.safetyCheckOpts`へ切り替え）、`src/chatgpt/selectors.ts`（該当3キーへ由来を示すコメント追加） | 決定・修正済み・Unit 169件通過（typecheck/lint/build clean）。ライブ再現テストは未実施（page.tsに既存のテストハーネスが無いため）。**docs/23-DURABLE-BRIDGE-PHASES.mdのPhase 0（17項目）が全て完了** |
| A-128 | 2026-09-18 | Bugfix | Phase 0-D-1(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`page.ts`の`normalisePrompt()`（composer入力確認の比較キー）が`.replace(/\s+/gu, "")`で全空白を除去しており、`print("a b")`と`print("ab")`、インデントの異なるコードを同一視していた。FR-024のコメント自体が「lossを検知するためであり、レイアウト差ではない」と述べているのに、実装は空白の意味的な差も含めて握りつぶしていた。ChatGPT実画面のProseMirror composerが連続空白をどう描画するかは実機検証していないため、行末正規化・NBSP等のNFKC正規化・前後端のtrimのみに絞り、それ以外の空白（インデント含む）は温存する最小限の修正にとどめた。ChatGPT実画面で予期しない空白の描画差分がある場合は`PROMPT_INPUT_FAILED`として新たに顕在化する可能性があり（安全側の失敗）、実機での確認が望ましい | `reviews/chatgpt-pro-self-review.md` #3 | `src/chatgpt/page.ts`（`normalisePrompt()`を新しい正規化ルールへ変更し、テスト容易化のため`export`化）、`tests/unit/page-normalise.test.ts`（新設。純関数のためpage.tsの他コードと異なりユニットテスト可能） | 決定・修正済み・Unit 174件通過（typecheck/lint/build clean）。ChatGPT実画面での確認は未実施 |
| A-129 | 2026-09-18 | Bugfix | Phase 0-D-2(`docs/23-DURABLE-BRIDGE-PHASES.md`)。`extraction/verify.ts`の`tokens()`は記号をすべて区切り文字として除去しており、`!=`と`==`が両方とも「トークン無し」に化けるため、演算子だけが異なるコードでもcoverage計算上は100%一致になり、`quality: "full"`判定を通ってしまい得た（再設計レビュー・fail-closedレビュー双方で指摘） | `reviews/chatgpt-pro-self-review.md` #4 | `src/extraction/verify.ts`（記号除去前の生テキストから主要な演算子（`==`/`!=`/`<=`/`>=`/`&&`/`||`等）を別途トークン化して単語トークン集合に追加する`operatorTokens()`を新設。既存の単語レベルの許容（markdown記法ノイズの無視等）はそのまま維持しつつ、演算子の不一致だけをcoverage計算へ反映させる）、`tests/unit/extraction.test.ts`（`!=`→`==`変化を検出する回帰テスト。fix無しで実際に通過してしまうことを確認済み） | 決定・修正済み・Unit 176件通過（typecheck/lint/build clean）。改行・インデント・token順序の保持までは未対応（別課題として残す） |
| A-130 | 2026-09-18 | Bugfix | Phase 0-E(`docs/23-DURABLE-BRIDGE-PHASES.md`)。A-113/A-114のemergency fallbackは`WRITE_RESULT`効果ハンドラが「既にterminal状態」で再入したときだけ発動する設計だったが、`WRITING_RESULT`（正常完了直前の非terminal状態）での`writeResult`失敗は`{writesResult:false}`でterminalへ遷移し、`WRITE_RESULT`効果を二度と発行しないため、fallbackへ到達せず**result.jsonが一切書かれないまま終わっていた**（再設計レビューで実証済み、A-112/A-113が対処しようとした「result.jsonが書かれない」問題の別の入口が残っていた） | `reviews/chatgpt-pro-redesign-review.md` §3.5 | `src/state/machine.ts`（`WRITING_RESULT`状態での`WRITE_FAILED(file:"result")`遷移を`writesResult:true`に変更し、`WRITE_RESULT`効果を再度発行する。これによりterminal状態で効果ハンドラが再入し、2回目の書き込みが失敗すればA-113/A-114のemergencyResult()フォールバックへ自然に到達する）、`tests/unit/machine.test.ts`・`tests/unit/controller.test.ts`（既存のwritesResult期待値を更新し、正常完了経路での書き込み失敗が実際にresult.jsonを生成することを検証する新規テストを追加。fix無しで両方とも実際に落ちる（旧: 呼び出し1回で終わり、リトライしない）ことを確認済み） | 決定・修正済み・Unit 178件通過（typecheck/lint/build clean）。**Phase 0残りは0-F(worker exit code分類)のみ** |
