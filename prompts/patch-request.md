# 修正パッチ依頼（unified diff）

request id: `{{requestId}}`

添付の {{ファイル名}} に対して、以下の修正を **unified diff（`git apply` できる形式、パスは `a/` `b/` 付き）** で出してください。diff 以外の説明は diff の後に短く。コードブロックは ```diff で囲ってください。

## 期待する動作
{{仕様を箇条書き}}

## 変えてはいけないこと
{{公開 API、既存テスト、フォーマット等}}

## 出力形式
1. 先頭に request id
2. ```diff ブロック（ファイルごとに 1 つ）
3. 変更点の要約（3 行以内）と、追加すべきテスト 1 つ
