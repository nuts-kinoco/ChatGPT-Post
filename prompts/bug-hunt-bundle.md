# 横断バグ探し（bundle 添付）

request id: `{{requestId}}`

添付の `context.md` は `chatgpt-bridge bundle` で作ったリポジトリの一部（ツリー + 各ファイルの本文）です。**モジュール間の不整合**（呼び出し側と実装の契約違反、エラー経路の取りこぼし、状態の二重管理、境界値の食い違い）を探してください。

## 焦点
{{例: 送信境界の安全性 / 秘密情報の扱い / 並行性}}

## 出力形式（Markdown）
1. 先頭に request id
2. 見つけた不整合を重要度順に: 関係するファイル:行 / 何が食い違うか / 再現条件 / 修正方針
3. bundle に無くて判断できなかったファイル名（あれば）
4. 「問題なし」と判断した領域とその根拠（短く）
